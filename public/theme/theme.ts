import { extendTheme } from '@chakra-ui/react';

const polzzakTheme = extendTheme({
  colors: {
    polzzak: {
      main: '#346CFD',
      sub: '#7800FF',
      normalGray: '#EEEEEE',
      lightGray: '#E0E0E0',
      black: '#222222',
      lightBlack: '#515151',
    },
  },
});

export default polzzakTheme;
